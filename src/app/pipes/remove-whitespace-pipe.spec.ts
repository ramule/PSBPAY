import { RemovewhitespacesPipe } from './remove-whitespace-pipe';

describe('RemovewhitespacesPipe', () => {
  it('create an instance', () => {
    const pipe = new RemovewhitespacesPipe();
    expect(pipe).toBeTruthy();
  });
});
