import { LimitDirectiveDirective } from './limit-directive.directive';

describe('LimitDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new LimitDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
