import { DatePatternDirective } from './date-pattern.directive';

describe('DatePatternDirective', () => {
  it('should create an instance', () => {
    const directive = new DatePatternDirective();
    expect(directive).toBeTruthy();
  });
});
